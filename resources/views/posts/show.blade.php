{{-- @extends('layouts.app')

@section('content')
    <h1>View Post</h1>

    <div class="card">
        <div class="card-header">
            <h5>{{ $post->judul }}</h5>
        </div>
        <div class="card-body">
            <p class="card-text">{{ $post->konten }}</p>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">Status: {{ $post->status_publish }}</li>
            <li class="list-group-item">Published At: {{ $post->tanggal_publikasi }}</li>
        </ul>
    </div>

    <a href="{{ route('posts.edit', $post) }}" class="btn btn-primary mt-3">Edit</a>
@endsection --}}
